# Managed by `chatpop admin add` / `chatpop admin remove`.
# Edit by hand only if you know what you're doing.
#
# This file IS committed to git — it's the team roster. Credentials for
# each developer live in terraform state (sensitive) and are never written
# to disk outside of state.

developers = [
  "robert",
]
