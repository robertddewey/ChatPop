# Frontend server port (MUST be 4000 per project standards)
PORT=4000

# NOTE: API and WebSocket URLs are handled automatically:
# - API calls use relative URLs and are proxied by server.js to backend
# - WebSocket connections are dynamically configured based on window.location.hostname
# No need to configure NEXT_PUBLIC_API_URL or NEXT_PUBLIC_WS_URL
